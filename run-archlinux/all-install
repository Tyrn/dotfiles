#!/usr/bin/env bash
# set -e
set -u
set -x

# ./rwasm-install
./base-install
#./extras-install
./spell-install
./nvim-install
# ./doom-install  # Probably outdated.
# ./qemu-install
# ./nix-init
# ./racket-install
./typst-install
chezmoi apply --force
#../gsettings-set
