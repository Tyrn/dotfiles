return {
  {
    "amekusa/auto-input-switch.nvim",
    lazy = false,
    opts = {
      os_settings = {
        linux = {
          -- cmd_get = "get_keyboard_layout_gnome", -- set the command that outputs the keyboard state
          -- cmd_set = "set_keyboard_layout_gnome %s", -- set the command that changes the keyboard state
        },
      },
    },
  },
}
